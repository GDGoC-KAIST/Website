{
  "name": "backend",
  "version": "1.0.0",
  "description": "Firebase 기반 백엔드 프로젝트입니다.",
  "main": "index.js",
  "scripts": {
    "firebase": "firebase",
    "firebase:init": "firebase init",
    "firebase:login": "firebase login",
    "firebase:deploy": "firebase deploy",
    "firebase:deploy:functions": "firebase deploy --only functions",
    "firebase:emulators": "firebase emulators:start",
    "firebase:emulators:functions": "firebase emulators:start --only functions",
    "firebase:emulators:stop": "pkill -f 'firebase.*emulator' || true; pkill -f 'firebase-database-emulator' || true; pkill -f 'cloud-firestore-emulator' || true; pkill -f 'cloud-storage-rules-runtime' || true; pkill -f 'functionsEmulatorRuntime' || true",
    "firebase:emulators:restart": "npm run firebase:emulators:stop && sleep 1 && firebase emulators:start",
    "docs:generate": "./generate-api-docs.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@apidevtools/json-schema-ref-parser": "^15.1.3",
    "@types/bcrypt": "^6.0.0",
    "@types/json2csv": "^5.0.7",
    "firebase-tools": "^13.0.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "json2csv": "^6.0.0-alpha.2",
    "jsonwebtoken": "^9.0.2",
    "react-markdown": "^10.1.0"
  }
}
