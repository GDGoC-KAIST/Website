rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 모든 파일에 대한 읽기/쓰기 허용 (개발용)
    // 프로덕션에서는 더 엄격한 규칙 적용 필요
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

